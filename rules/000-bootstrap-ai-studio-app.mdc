Initialize a new Next.js + Tauri project following the attached "000-next-tauri-rules" and "002-cursor-rules-logger".
All other rules also is MUST READ, FOLLOW.

`000-general-rules`, `001-tasks-workflow`, `003-documentation-context`, `005-build-automation`

Steps to execute:
1.  **Scaffold**: Create root config files. Setup `src-ts` for Next.js (clean generated boilerplate) and `src-tauri` for Rust.
2.  **Config**: Ensure `next.config.js` points to `src-ts` correctly and sets `output: 'export'` for Tauri compatibility.
3.  **Bridge**: Implement `src-ts/lib/logger.ts` immediately based on Rule 002.
4.  **Component**: Add a generic Layout in `src-ts` utilizing `21st.dev` style components.
5.  **Build Check**: Verify strict separation of concerns (No Node.js APIs in React side).

Constraints:
- Do not hallucinate packages. Use standard `pnpm` or `npm`.
- Ensure directory structure `src-ts` vs `src-tauri` is strictly enforced.
